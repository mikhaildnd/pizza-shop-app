import { FC } from 'react';
import { CartBlock } from '../components';

const Cart: FC = () => {
  return (
    <div className="container container--cart">
      <CartBlock />
    </div>
  );
};

export default Cart;
